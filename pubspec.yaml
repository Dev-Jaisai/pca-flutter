name: textewidget
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.0  # <--- Add this
  http: ^1.1.0
  intl: ^0.17.0
  cupertino_icons: ^1.0.8
  rename: ^3.1.0
  url_launcher: ^6.1.0   # MUST BE inside dependencies, not dev_dependencies
  shimmer: ^3.0.0
  cached_network_image: ^3.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  font_awesome_flutter: ^10.7.0
  google_fonts: ^6.3.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/cricket_intro.png
    - assets/images/cricket_bg.webp   # UPDATED

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
